#!/bin/sh

if [ -z "${2}" ]; then 
    DIR='/home/pl/dotfiles'
else 
    DIR=${2}
fi


xfce4-terminal \
  -e "distrobox enter arch -e 'nvim $1'" \
  -T "nvim $1" \
  --default-working-directory="$DIR"
